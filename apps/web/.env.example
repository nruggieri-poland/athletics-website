PAYLOAD_URL=http://localhost:3000
# Same value as apps/cms/.env's PAYLOAD_BUILD_API_KEY — lets the build read
# the (otherwise admin-only) Redirects collection to generate /go/[slug] pages.
PAYLOAD_BUILD_API_KEY=changeme-generate-a-random-key-shared-with-the-web-build
